dist: xenial
language: python
python:
  - "3.7"
service:
  - docker
install:
  - pip install -e .
  - pip install codecov
script:
  - coverage run setup.py test
  - tests/e2e/run.sh chrome
  - tests/e2e/run.sh firefox
after_success:
  - codecov
